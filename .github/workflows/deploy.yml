name: Build and Deploy

on:
  pull_request:
    types: [ closed ]
    branches: master

jobs:
  publish:
    name: Publish packages
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v2.3.4
      - name: Set up JDK 14
        uses: actions/setup-java@v1
        with:
          java-version: 14.0.1
      - name: Configure git
        run: |
          git config --local user.email "release@foreignlanguagereader.com"
          git config --local user.name "Release"
      - name: Publish
        run: sbt githubRelease

  build:
    runs-on: ubuntu-latest
    name: Build API
    steps:
    - uses: actions/checkout@v2.3.4

    - name: Save DigitalOcean kubeconfig
      uses: digitalocean/action-doctl@v2.1.0
      with:
        token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}
    
    - name: Build container
      run: docker build . -t api

    - name: Enforce security rules
      id: scan
      uses: anchore/scan-action@v2
      with:
        image: "api:latest"
        fail-build: true
        acs-report-enable: true

    - name: Log into docker
      run: doctl registry login

    - name: Push container
      run: |
        docker tag api:latest registry.digitalocean.com/foreign-language-reader/api:latest
        docker push registry.digitalocean.com/foreign-language-reader/api:latest

  deploy:
    name: Deploy API
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - uses: actions/checkout@v2.3.4

      - name: Save DigitalOcean kubeconfig
        uses: digitalocean/action-doctl@v2.1.0
        with:
          args: kubernetes cluster kubeconfig show foreign-language-reader > $GITHUB_WORKSPACE/.kubeconfig
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Update image in K8s
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: foreign-language-reader-api
          IMAGE_TAG: ${{ github.sha }}
        run: |
          kubectl --kubeconfig=$GITHUB_WORKSPACE/.kubeconfig \
            set image deployment/api api=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG --record

      - name: Wait for deployment to finish
        run: |
          kubectl --kubeconfig=$GITHUB_WORKSPACE/.kubeconfig \
            rollout status deployment/api